<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script type="text/javascript" src="http://code.jquery.com/jquery-1.12.4.min.js"></script>
</head>
<body>
    <h1>get으로 정보 받기</h1>
    <form action="/get" method="get" target="_blank" name="new">
        <p>이름<input type="text" name="name" /></p>
        <fieldset >
            <legend>성별</legend>
            <input type="radio" name="gender" value="남자" id="men" /> <label for="men">남자</label>   
            <input type="radio" name="gender" value="여자" id="women" /> <label for="women">여자</label> 
        </fieldset>
        <fieldset>
            <legend>생년월일</legend>
            <select name="year">
                <% for(let i=2023; i>=1970; i--){  %>
                    <option value="<%= i %>"> <%= i %></option>
                <% } %>
            </select>년
            <select name="month">
                <% for(let i=1; i<=12; i++){  %>
                    <option value="<%= i %>"> <%= i %></option>
                <% } %>
            </select>월
            <select name="day">
                <% for(let i=1; i<=31; i++){  %>
                    <option value="<%= i %>"> <%= i %></option>
                <% } %>
            </select>일
        </fieldset>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="like" value="여행" id="travel" /> <label for="travel">여행</label>   
            <input type="checkbox" name="like" value="패션" id="fashion" /> <label for="fashion">패션</label>   
            <input type="checkbox" name="like" value="음식" id="food" /> <label for="food">음식</label>   
        </fieldset>

        <button type="submit">회원가입</button>   
    </form>

    <h1>post로 정보 받기</h1>
    <form action="/post" method="post">
        <p>이름<input type="text" name="name" /></p>
        <fieldset >
            <legend>성별</legend>
            <input type="radio" name="gender" value="남자" id="men_post" /> <label for="men_post">남자</label>   
            <input type="radio" name="gender" value="여자" id="women_post" /> <label for="women_post">여자</label> 
        </fieldset>
        <fieldset>
            <legend>생년월일</legend>
            <select name="year">
                <% for(let i=2023; i>=1970; i--){  %>
                    <option value="<%= i %>"> <%= i %></option>
                <% } %>
            </select>년
            <select name="month">
                <% for(let i=1; i<=12; i++){  %>
                    <option value="<%= i %>"> <%= i %></option>
                <% } %>
            </select>월
            <select name="day">
                <% for(let i=1; i<=31; i++){  %>
                    <option value="<%= i %>"> <%= i %></option>
                <% } %>
            </select>일
        </fieldset>
        <fieldset>
            <legend>관심사</legend>
            <input type="checkbox" name="like_post" value="여행" id="travel_post" /> <label for="travel_post">여행</label>   
            <input type="checkbox" name="like_post" value="패션" id="fashion_post" /> <label for="fashion_post">패션</label>   
            <input type="checkbox" name="like_post" value="음식" id="food_post" /> <label for="food_post">음식</label>   
        </fieldset>

        <button type="submit">회원가입</button>   
    </form>

    <h1>post 회원가입 validation 적용</h1>
    <form action="/post/validation" method="post">
        <p>아이디 <input type="text" name="id3" pattern="^[a-z0-9]{5,}$" title="영어 소문자, 숫자만 사용 가능(5글자 이상)"/></p>
        <p>비밀번호 <input type="password" name="pw3" pattern="^(?=.*?[0-9])(?=.*?[#?!@$ %^&*-]).{8,}$" title="영어 소문자, 대문자, 숫자, 특수 문자를 모두 한 번 이상은 사용 (8글자 이상)" required /> </p>
        <p>전화번호 <input type="text" pattern="^\d{3}-\d{3,4}-\d{4}$" title="010-000-000 형식" required /></p>
        <p>이름 <input type="text" name="name3" required /></p>
        <p>성별 <input type="text" name="gender3" required /></p>
        <button type="submit">회원가입</button>   
    </form>




</body>
</html>